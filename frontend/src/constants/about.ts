/** The two class strings every band of the landing page shares: its inset from the
 * viewport edges, and the measure its prose is held to.
 */

export const INSET = 'px-5 sm:px-8';

export const MEASURE = 'max-w-[68ch]';
